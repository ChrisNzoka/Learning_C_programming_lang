#include <stdio.h>

/**
 *
 *
 *
 */

int main(void)
{
    int hex;

    puts("Decimal \t Hex value");
    for (hex = 9; hex <= 20; hex++)
    {
    printf("%d \t %X\n", hex, hex);
    }
    printf("Uppercase Hex values");
    return (0);
}
